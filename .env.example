# Copy to .env.local and set your own values.
# The password your mom types to open the admin dashboard:
ADMIN_PASSWORD=change-me-to-something-strong
# A long random secret used to sign the admin session (any 30+ random characters):
ADMIN_TOKEN=replace-with-a-long-random-string
